# Shell scripts run inside Linux containers — a CRLF shebang line
# ("#!/usr/bin/env bash\r") fails with "bash\r: No such file or directory".
# Force LF regardless of core.autocrlf so Windows checkouts don't silently
# break every Docker build that COPYs these in.
*.sh text eol=lf
Dockerfile text eol=lf
